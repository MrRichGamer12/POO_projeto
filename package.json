{
  "name": "poo-projeto-3-foflux",
  "version": "1.1.0",
  "description": "Aplicação Foflux para POO com localStorage e JSON Server como Mock Server.",
  "private": true,
  "scripts": {
    "mock-server": "json-server --watch db.json --port 3000",
    "mock-server:delay": "json-server --watch db.json --port 3000 --delay 500"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  },
  "type": "module"
}
